---
phase: 04-advanced-deep-dives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/topics.ts
  - packages/vue-app/src/topics/scaling-up/Routing.vue
  - packages/vue-app/src/topics/scaling-up/StateManagement.vue
  - packages/vue-app/src/topics/scaling-up/Testing.vue
  - packages/vue-app/src/topics/scaling-up/Typescript.vue
  - packages/react-app/src/topics/scaling-up/Routing.tsx
  - packages/react-app/src/topics/scaling-up/StateManagement.tsx
  - packages/react-app/src/topics/scaling-up/Testing.tsx
  - packages/react-app/src/topics/scaling-up/Typescript.tsx
autonomous: true
requirements:
  - SCAL-01
  - SCAL-02
  - SCAL-03
  - SCAL-04

must_haves:
  truths:
    - "Sidebar shows 'Scaling Up' category with 4 topics (Routing, State Management, Testing, TypeScript)"
    - 'Each Scaling Up topic has live demo + code toggle + explanation on both Vue and React sides'
    - 'No new npm packages installed — demos use existing primitives (ref/useState) with code snippets showing real library APIs'
  artifacts:
    - path: 'packages/shared/src/topics.ts'
      provides: '7 new topic registry entries (4 scaling-up + 3 deep-dive)'
      contains: "category: 'scaling-up'"
    - path: 'packages/vue-app/src/topics/scaling-up/Routing.vue'
      provides: 'Vue Routing topic demo'
      contains: 'Vue Router'
    - path: 'packages/react-app/src/topics/scaling-up/Routing.tsx'
      provides: 'React Routing topic demo'
      contains: 'React Router'
    - path: 'packages/vue-app/src/topics/scaling-up/StateManagement.vue'
      provides: 'Vue State Management topic demo'
      contains: 'Pinia'
    - path: 'packages/react-app/src/topics/scaling-up/StateManagement.tsx'
      provides: 'React State Management topic demo'
      contains: 'Zustand'
    - path: 'packages/vue-app/src/topics/scaling-up/Testing.vue'
      provides: 'Vue Testing topic demo'
      contains: 'Vue Test Utils'
    - path: 'packages/react-app/src/topics/scaling-up/Testing.tsx'
      provides: 'React Testing topic demo'
      contains: 'React Testing Library'
    - path: 'packages/vue-app/src/topics/scaling-up/Typescript.vue'
      provides: 'Vue TypeScript topic demo'
      contains: 'defineProps'
    - path: 'packages/react-app/src/topics/scaling-up/Typescript.tsx'
      provides: 'React TypeScript topic demo'
      contains: 'interface'
  key_links:
    - from: 'packages/shared/src/topics.ts'
      to: 'TopicRenderer glob'
      via: 'registry slug matches PascalCase filename'
      pattern: "category: 'scaling-up'"
---

<objective>
Register 7 new topics in shared registry and create 4 Scaling Up topic files for both Vue and React.

Purpose: Complete the Scaling Up category (Routing, State Management, Testing, TypeScript) — topics about ecosystem tools that complement each framework.
Output: 9 files — 1 registry update + 4 Vue topic files + 4 React topic files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-deep-dives/04-RESEARCH.md

@packages/shared/src/topics.ts
@packages/shared/src/types.ts
@packages/shared/src/constants.ts
@packages/react-app/src/topics/built-in/Transition.tsx
@packages/vue-app/src/topics/built-in/Transition.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry expansion + 4 Vue Scaling Up topics</name>
  <files>
    packages/shared/src/topics.ts,
    packages/vue-app/src/topics/scaling-up/Routing.vue,
    packages/vue-app/src/topics/scaling-up/StateManagement.vue,
    packages/vue-app/src/topics/scaling-up/Testing.vue,
    packages/vue-app/src/topics/scaling-up/Typescript.vue
  </files>
  <read_first>
    packages/shared/src/topics.ts,
    packages/shared/src/types.ts,
    packages/shared/src/constants.ts,
    packages/vue-app/src/topics/built-in/Transition.vue
  </read_first>
  <action>
**1. Registry expansion** — Add 7 new entries to the `topics` array in `packages/shared/src/topics.ts`, after the existing `suspense` entry:

```typescript
// --- Scaling Up ---
{ id: 'routing', slug: 'routing', title: 'Routing', category: 'scaling-up', order: 1, description: 'Vue Router vs React Router' },
{ id: 'state-management', slug: 'state-management', title: 'State Management', category: 'scaling-up', order: 2, description: 'Pinia vs Zustand' },
{ id: 'testing', slug: 'testing', title: 'Testing', category: 'scaling-up', order: 3, description: 'Vitest + Vue Test Utils vs React Testing Library' },
{ id: 'typescript', slug: 'typescript', title: 'TypeScript', category: 'scaling-up', order: 4, description: 'defineProps<T>/defineEmits<T> vs React props typing' },
// --- Deep Dive ---
{ id: 'rendering-mechanism', slug: 'rendering-mechanism', title: 'Rendering Mechanism', category: 'deep-dive', order: 1, description: 'Vue compiler optimized diff vs React full subtree diff' },
{ id: 'reactivity-in-depth', slug: 'reactivity-in-depth', title: 'Reactivity In-Depth', category: 'deep-dive', order: 2, description: 'Proxy-based tracking vs immutable state comparison' },
{ id: 'rerender-optimization', slug: 'rerender-optimization', title: 'Re-render & Optimization', category: 'deep-dive', order: 3, description: 'Vue auto optimization vs React manual (memo, useMemo, useCallback)' },
```

**2. Create 4 Vue Scaling Up topic files** in `packages/vue-app/src/topics/scaling-up/`:

All 4 follow the locked pattern: `<script setup lang="ts">` + demo box (border border-slate-200 rounded-lg p-4 mb-4) + code toggle + explanation box (p-4 bg-slate-50 rounded).

**Routing.vue (SCAL-01):** Vue Router code snippet demo. Live demo: simplified simulated router using `ref<string>` for current route, buttons to navigate between "pages". DEMO_CODE shows actual Vue Router setup (`createRouter`, `useRouter`, `<RouterView>`, `<RouterLink>`). Explanation: Vue Router is tightly integrated (createRouter + app.use) vs React Router uses JSX components (`<BrowserRouter>`, `<Routes>`, `<Route>`). Both support nested routes, params, guards. Vue has navigation guards as router-level config, React has loaders/actions in v7.

**StateManagement.vue (SCAL-02):** Pinia code snippet demo. Live demo: simplified store simulation using `ref()` for count + `computed()` for double — shows Pinia-like `defineStore` API in DEMO_CODE. No Pinia installed. Explanation: Pinia = official Vue state, `defineStore()` returns composable-like API with `state`/`getters`/`actions`. React equivalent: Zustand uses `create()` returning a hook. Both are lightweight. Vuex is deprecated, Redux is heavy compared to Zustand.

**Testing.vue (SCAL-03):** Code display topic. Live demo: show two code blocks side-by-side — a Vue component and its test file using `mount()` from `@vue/test-utils` + `vi.fn()`. A "Run" button toggles showing simulated green test output. DEMO_CODE shows the test file content. Explanation: Vue uses `@vue/test-utils` `mount()`/`shallowMount()`, React uses `@testing-library/react` `render()`/`screen`. Both run on Vitest. Key diff: VTU has `.find()`, `.trigger()` vs RTL has `getByRole()`, `fireEvent()`. RTL encourages testing from user perspective (accessibility queries).

**Typescript.vue (SCAL-04):** Live demo: a typed component using `defineProps<{ name: string; count: number }>()` + `defineEmits<{ increment: [value: number] }>()`. Show the component rendering with typed props. DEMO_CODE shows the full typed Vue SFC. Explanation: Vue uses compiler macros `defineProps<T>()` and `defineEmits<T>()` for type inference directly in `<script setup>`. React types props directly: `function MyComp(props: { name: string })`. Vue needs `defineComponent()` wrapper when not in SFC. Both have full TypeScript support; Vue's macros are more concise.

Run `pnpm prettier --write` on all created/modified files.
</action>
<verify>
<automated>ls packages/vue-app/src/topics/scaling-up/\*.vue | wc -l && grep -c "category: 'scaling-up'" packages/shared/src/topics.ts && grep -c "category: 'deep-dive'" packages/shared/src/topics.ts</automated>
</verify>
<acceptance_criteria> - `grep -c "category: 'scaling-up'" packages/shared/src/topics.ts` returns 4 - `grep -c "category: 'deep-dive'" packages/shared/src/topics.ts` returns 3 - `ls packages/vue-app/src/topics/scaling-up/` shows exactly: Routing.vue, StateManagement.vue, Testing.vue, Typescript.vue - Each Vue file contains `<script setup lang="ts">`, `showCode`, `DEMO_CODE`, `bg-slate-50 rounded` - `grep -l "Vue Router" packages/vue-app/src/topics/scaling-up/Routing.vue` returns match - `grep -l "Pinia" packages/vue-app/src/topics/scaling-up/StateManagement.vue` returns match - `grep -l "vue/test-utils" packages/vue-app/src/topics/scaling-up/Testing.vue` returns match (in DEMO_CODE) - `grep -l "defineProps" packages/vue-app/src/topics/scaling-up/Typescript.vue` returns match
</acceptance_criteria>
<done>Topic registry has 33 entries (26 existing + 7 new). 4 Vue Scaling Up topic files exist with demo + code toggle + explanation following locked pattern.</done>
</task>

<task type="auto">
  <name>Task 2: 4 React Scaling Up topics</name>
  <files>
    packages/react-app/src/topics/scaling-up/Routing.tsx,
    packages/react-app/src/topics/scaling-up/StateManagement.tsx,
    packages/react-app/src/topics/scaling-up/Testing.tsx,
    packages/react-app/src/topics/scaling-up/Typescript.tsx
  </files>
  <read_first>
    packages/react-app/src/topics/built-in/Transition.tsx,
    packages/vue-app/src/topics/scaling-up/Routing.vue,
    packages/vue-app/src/topics/scaling-up/StateManagement.vue,
    packages/vue-app/src/topics/scaling-up/Testing.vue,
    packages/vue-app/src/topics/scaling-up/Typescript.vue
  </read_first>
  <action>
Create 4 React Scaling Up topic files in `packages/react-app/src/topics/scaling-up/`. Each mirrors its Vue counterpart, following the locked React topic pattern: `export default function TopicName()` + `useState` for showCode + DEMO_CODE const + demo box (className="border border-slate-200 rounded-lg p-4 mb-4") + code toggle + explanation box (className="p-4 bg-slate-50 rounded").

**Routing.tsx (SCAL-01):** React Router code snippet demo. Live demo: simplified simulated router using `useState<string>` for current route, buttons to switch "pages". DEMO_CODE shows actual React Router setup (`<BrowserRouter>`, `<Routes>`, `<Route>`, `useNavigate`, `useParams`). Import from `'react-router'` only (not react-router-dom per project convention). Explanation: React Router v7 uses JSX to declare routes (`<Route path="..." element={...} />`). Vue Router uses JS config array (`routes: [{ path, component }]`). React Router has `<Link>`, `useNavigate()`, `useParams()`. Both support nested routes and dynamic segments.

**StateManagement.tsx (SCAL-02):** Zustand code snippet demo. Live demo: simplified store simulation using `useState` for count + derived double via useMemo. DEMO_CODE shows actual Zustand `create()` API. No Zustand installed. Explanation: Zustand `create()` returns a hook — call `useStore()` in any component. Pinia `defineStore()` returns composable. Both are lightweight, no provider wrappers needed. Redux Toolkit is heavier but standard for large React apps. Vuex is deprecated.

**Testing.tsx (SCAL-03):** Code display topic. Live demo: show two code blocks — a React component and its test file using `render()` from `@testing-library/react` + `screen.getByRole()`. A "Run" button toggles showing simulated green test output. DEMO_CODE shows the test file content. Explanation: RTL uses `render()` + `screen` queries, VTU uses `mount()` + wrapper methods. RTL's philosophy: test as user interacts (by role, text, label). VTU allows both user-centric and implementation-centric (`.find('.class')`). Both run on Vitest.

**Typescript.tsx (SCAL-04):** Live demo: a typed component with `interface Props { name: string; count: number }` + `function TypedComponent(props: Props)`. Show it rendering. DEMO_CODE shows the full typed React component with props interface and event handler typing. Explanation: React types props directly via interface/type on function params. No compiler macro needed. Vue's `defineProps<T>()` does the same but as a compile-time macro in SFC. For events: Vue has `defineEmits<T>()`, React types callback props directly (`onClick: (value: number) => void`).

Run `pnpm prettier --write` on all created files.
</action>
<verify>
<automated>ls packages/react-app/src/topics/scaling-up/\*.tsx | wc -l && pnpm -r typecheck 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - `ls packages/react-app/src/topics/scaling-up/` shows exactly: Routing.tsx, StateManagement.tsx, Testing.tsx, Typescript.tsx - Each file contains `export default function`, `useState`, `DEMO_CODE`, `bg-slate-50 rounded` - `grep -l "React Router" packages/react-app/src/topics/scaling-up/Routing.tsx` returns match - `grep -l "Zustand" packages/react-app/src/topics/scaling-up/StateManagement.tsx` returns match - `grep -l "testing-library" packages/react-app/src/topics/scaling-up/Testing.tsx` returns match (in DEMO_CODE) - `grep -l "interface" packages/react-app/src/topics/scaling-up/Typescript.tsx` returns match - `pnpm -r typecheck` passes with 0 errors
</acceptance_criteria>
<done>4 React Scaling Up topic files exist mirroring Vue counterparts. TypeScript check passes across all packages.</done>
</task>

</tasks>

<verification>
- `grep -c "category: 'scaling-up'" packages/shared/src/topics.ts` returns 4
- `grep -c "category: 'deep-dive'" packages/shared/src/topics.ts` returns 3
- `ls packages/vue-app/src/topics/scaling-up/ | wc -l` returns 4
- `ls packages/react-app/src/topics/scaling-up/ | wc -l` returns 4
- `pnpm -r typecheck` passes with 0 errors
- `pnpm dev` starts and navigating to `/scaling-up/routing` renders both panels
</verification>

<success_criteria>
Registry has 33 entries. 4 Scaling Up topics render on both Vue and React sides with demo + code toggle + explanation. No new npm packages added. TypeScript passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-deep-dives/04-01-SUMMARY.md`
</output>
