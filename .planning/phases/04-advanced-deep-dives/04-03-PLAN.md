---
phase: 04-advanced-deep-dives
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - SCAL-01
  - SCAL-02
  - SCAL-03
  - SCAL-04
  - DEEP-01
  - DEEP-02
  - DEEP-03

must_haves:
  truths:
    - "All 7 new topics render correctly on both Vue and React sides"
    - "Sidebar shows 'Scaling Up' (4 topics) and 'Deep Dive' (3 topics) categories"
    - "Deep Dive diagrams are visible and readable in both panels"
    - "TypeScript check passes with 0 errors"
    - "No new npm packages were added"
  artifacts: []
  key_links:
    - from: "packages/shared/src/topics.ts"
      to: "Sidebar"
      via: "getAllCategories + getTopicsByCategory"
      pattern: "scaling-up|deep-dive"
---

<objective>
End-to-end verification of all Phase 4 deliverables: 4 Scaling Up topics + 3 Deep Dive topics with diagrams.

Purpose: Confirm all 7 new topics are accessible, interactive, and correctly displayed before marking Phase 4 complete.
Output: Verification results confirming phase success criteria
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-advanced-deep-dives/04-01-SUMMARY.md
@.planning/phases/04-advanced-deep-dives/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all Phase 4 deliverables</name>
  <files></files>
  <read_first>
    packages/shared/src/topics.ts,
    packages/vue-app/src/topics/scaling-up/Routing.vue,
    packages/react-app/src/topics/scaling-up/Routing.tsx,
    packages/vue-app/src/topics/deep-dive/RenderingMechanism.vue,
    packages/react-app/src/topics/deep-dive/RenderingMechanism.tsx
  </read_first>
  <action>
Run the following verification checks:

1. **Registry check**: Confirm `packages/shared/src/topics.ts` has exactly 33 entries (26 old + 7 new). Confirm 4 entries with `category: 'scaling-up'` and 3 with `category: 'deep-dive'`.

2. **File existence**: Confirm all 14 new topic files exist:
   - `packages/vue-app/src/topics/scaling-up/{Routing,StateManagement,Testing,Typescript}.vue`
   - `packages/react-app/src/topics/scaling-up/{Routing,StateManagement,Testing,Typescript}.tsx`
   - `packages/vue-app/src/topics/deep-dive/{RenderingMechanism,ReactivityInDepth,RerenderOptimization}.vue`
   - `packages/react-app/src/topics/deep-dive/{RenderingMechanism,ReactivityInDepth,RerenderOptimization}.tsx`

3. **Pattern compliance**: Each file contains:
   - Demo box: `border border-slate-200 rounded-lg p-4`
   - Code toggle: `showCode` / `setShowCode`
   - Explanation: `bg-slate-50 rounded`
   - Deep Dive files additionally contain: `<svg` with `viewBox` and `aria-label`

4. **SVG marker uniqueness**: No two SVG files share the same marker ID. Grep all `id="arrow-` patterns across deep-dive folders and confirm 6 unique IDs.

5. **TypeScript check**: `pnpm -r typecheck` passes with 0 errors.

6. **No new packages**: `git diff HEAD -- package.json pnpm-lock.yaml` shows no new dependencies added (only existing lockfile changes from other work are acceptable, but no new entries in dependencies/devDependencies).

7. **Dev server smoke test**: Start `pnpm dev`, confirm it starts without errors. (If dev server already running, just confirm no build errors.)

Report results as a checklist. If any check fails, document the failure for human review.
  </action>
  <verify>
    <automated>pnpm -r typecheck 2>&1 | tail -3 && echo "---" && ls packages/vue-app/src/topics/scaling-up/*.vue packages/react-app/src/topics/scaling-up/*.tsx packages/vue-app/src/topics/deep-dive/*.vue packages/react-app/src/topics/deep-dive/*.tsx 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -r typecheck` exits with code 0
    - 14 new topic files exist (4+4 scaling-up + 3+3 deep-dive)
    - `grep -c "category:" packages/shared/src/topics.ts` returns 33
    - `grep -rc "svg" packages/vue-app/src/topics/deep-dive/ packages/react-app/src/topics/deep-dive/` shows all 6 files contain SVG
  </acceptance_criteria>
  <done>All automated checks pass. Phase 4 deliverables verified programmatically.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 4 topics</name>
  <files></files>
  <action>
Present the user with verification steps for visual inspection of all 7 new topics.

What was built:
- Scaling Up: Routing, State Management, Testing, TypeScript (4 topics, code-snippet demos)
- Deep Dive: Rendering Mechanism, Reactivity In-Depth, Re-render and Optimization (3 topics with inline SVG diagrams)

All topics follow the established demo box + code toggle + explanation pattern. Deep Dive topics add a diagram section below the explanation.

How to verify:
1. Run `pnpm dev` and open http://localhost:5173
2. Scaling Up category — click each of the 4 topics, confirm both sides show demo + explanation
3. Deep Dive category — click each of the 3 topics, confirm both sides show demo + explanation + SVG diagram below
4. Diagram quality: readable at panel width, responsive on resize, arrow markers render correctly, text legible
5. Sidebar: "Scaling Up" shows 4 topics, "Deep Dive" shows 3 topics, total registry count is 33
  </action>
  <verify>User types "approved" or describes issues</verify>
  <acceptance_criteria>
    - User confirms all 7 topics render correctly on both sides
    - User confirms diagrams are visible and readable
    - User confirms sidebar categories display correctly
  </acceptance_criteria>
  <done>User approves visual quality of all Phase 4 topics and diagrams.</done>
</task>

</tasks>

<verification>
All Phase 4 success criteria from ROADMAP.md:
1. 4 Scaling Up topics (Routing, State Management, Testing, TypeScript) have live demo + explanation
2. Deep Dive page Rendering Mechanism has diagram (Vue compiler diff vs React full subtree diff)
3. Deep Dive page Reactivity In-Depth has diagram (Proxy-based tracking vs immutable state)
4. Deep Dive page Re-render & Optimization has diagram (Vue auto vs React manual memo/useMemo/useCallback)
5. Deep Dive layout maintains demo area above, diagram section below
</verification>

<success_criteria>
Phase 4 complete: all 7 new topics render correctly on both sides, diagrams are visible and readable, TypeScript passes, no new packages added. User approves visual quality.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-deep-dives/04-03-SUMMARY.md`
</output>
