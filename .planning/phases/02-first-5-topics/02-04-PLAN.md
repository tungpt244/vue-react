---
phase: 02-first-5-topics
plan: 04
type: execute
wave: 3
depends_on: ['02-02', '02-03']
files_modified: []
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - DISP-01
  - DISP-02
  - ESS-01
  - ESS-02
  - ESS-03
  - ESS-08
  - ESS-10
  - ESS-11

must_haves:
  truths:
    - 'All 6 topics accessible via sidebar and render correctly on both panels'
    - 'Each topic has working interactive demo'
    - 'Each topic has explanation text'
    - 'Sidebar navigation works end-to-end'
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of all Phase 2 deliverables: sidebar navigation, 6 interactive topics, and side-by-side layout.

Purpose: Confirm everything works together before closing Phase 2.
Output: Verified working state or list of issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-first-5-topics/02-CONTEXT.md
@.planning/phases/02-first-5-topics/02-01-SUMMARY.md
@.planning/phases/02-first-5-topics/02-02-SUMMARY.md
@.planning/phases/02-first-5-topics/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all Phase 2 deliverables</name>
  <files></files>
  <read_first>
    packages/shared/src/topics.ts,
    packages/react-app/src/components/Sidebar.tsx,
    packages/react-app/src/components/Layout.tsx,
    packages/host/src/styles.css
  </read_first>
  <action>
Run full verification suite:

1. **TypeScript check:** `pnpm -r typecheck` must pass with 0 errors
2. **Topic registry:** Verify `packages/shared/src/topics.ts` has exactly 6 entries with ids: template-syntax, reactivity, computed, watchers, lifecycle, class-style-bindings
3. **File existence check:** Verify all 12 topic files exist:
   - `packages/react-app/src/topics/essentials/TemplateSyntax.tsx`
   - `packages/vue-app/src/topics/essentials/TemplateSyntax.vue`
   - `packages/react-app/src/topics/essentials/Reactivity.tsx`
   - `packages/vue-app/src/topics/essentials/Reactivity.vue`
   - `packages/react-app/src/topics/essentials/Computed.tsx`
   - `packages/vue-app/src/topics/essentials/Computed.vue`
   - `packages/react-app/src/topics/essentials/Watchers.tsx`
   - `packages/vue-app/src/topics/essentials/Watchers.vue`
   - `packages/react-app/src/topics/essentials/Lifecycle.tsx`
   - `packages/vue-app/src/topics/essentials/Lifecycle.vue`
   - `packages/react-app/src/topics/essentials/ClassStyleBindings.tsx`
   - `packages/vue-app/src/topics/essentials/ClassStyleBindings.vue`
4. **Content check:** Each topic file is not a placeholder (contains more than 10 lines, has interactive elements like `useState`/`ref(`/`onClick`/`@click`)
5. **Sidebar exists:** `packages/react-app/src/components/Sidebar.tsx` exists and contains `useNavigate`
6. **Dev server starts:** `pnpm dev` starts without build errors (start, wait 5s, check for errors, kill)

Report results. If any check fails, fix it.
</action>
<verify>
<automated>cd /home/tungpt244/workspace/vibe && pnpm -r typecheck 2>&1 | tail -3</automated>
</verify>
<acceptance_criteria> - `pnpm -r typecheck` exits 0 - All 12 topic files exist and have more than 10 lines each - packages/shared/src/topics.ts contains 6 topic entries - Dev server starts without errors
</acceptance_criteria>
<done>All automated checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 2</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for confirmation. The dev server should already be running from Task 1 verification. If not, start it with `pnpm dev`.
  </action>
  <verify>User confirms approval or provides issues list</verify>
  <acceptance_criteria>
    - User types "approved" indicating all 6 topics render correctly with interactive demos and explanations on both Vue and React panels
  </acceptance_criteria>
  <done>User has visually verified all Phase 2 deliverables work end-to-end.</done>
  <what-built>
    Sidebar navigation with 6 Essentials topics, each having interactive demos on both Vue and React panels displayed side-by-side.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:5173
    2. **Sidebar:** Left sidebar shows "Essentials" category with 6 topics listed. Clicking collapse toggle hides/shows the sidebar.
    3. **Template/JSX:** Click "Template vs JSX" -- both panels show profile card with show/hide toggle and skills list
    4. **Reactivity:** Click "Reactivity" -- both panels show counter with +/- buttons and form inputs with live updates
    5. **Computed:** Click "Computed / useMemo" -- both panels show shopping list with computed totals
    6. **Watchers:** Click "Watchers / useEffect" -- both panels show search + question input with log panel
    7. **Lifecycle:** Click "Lifecycle Hooks" -- both panels show timer with mount/unmount toggle and log
    8. **Class & Style:** Click "Class & Style Bindings" -- both panels show theme switcher with dynamic styling
    9. **Each topic:** Verify explanation text appears below the demo
    10. **Navigation:** Active topic is highlighted in sidebar. Switching topics loads correct content on both sides.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All Phase 2 success criteria verified:
1. Sidebar displays topic list grouped by category, click navigates, both sides render
2. Each topic has interactive demo responding to user input
3. Each topic has explanation text
4. 6 topics operational: Template/JSX, Reactivity, Computed, Watchers, Lifecycle, Class & Style Bindings
</verification>

<success_criteria>

- User confirms all 6 topics work with interactive demos on both sides
- Sidebar navigation functional
- Side-by-side layout intact
  </success_criteria>

<output>
After completion, create `.planning/phases/02-first-5-topics/02-04-SUMMARY.md`
</output>
