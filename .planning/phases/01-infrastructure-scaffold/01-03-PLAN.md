---
phase: 01-infrastructure-scaffold
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - packages/host/src/styles.css
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07

must_haves:
  truths:
    - 'pnpm dev starts and both #vue-root and #react-root render on the same page'
    - 'Editing a .vue file triggers HMR only for Vue side; editing .tsx triggers HMR only for React side'
    - 'Navigating to URL /:category/:topicId renders correct topic on both sides, including on direct URL refresh'
    - 'pnpm -r typecheck passes on all 4 packages with zero errors'
    - 'Topic registry from @vibe/shared is imported and used in both vue-app and react-app'
  artifacts:
    - path: 'packages/host/src/styles.css'
      provides: 'Side-by-side grid layout for #app-shell'
      contains: 'grid-template-columns'
  key_links:
    - from: 'packages/host/src/main.ts'
      to: 'packages/vue-app/src/main.ts, packages/react-app/src/main.tsx'
      via: 'import boot'
      pattern: 'import.*@vibe'
    - from: 'packages/react-app/src/hooks/useRouteDispatch.ts'
      to: 'packages/vue-app/src/composables/useRouteSync.ts'
      via: 'CustomEvent on window'
      pattern: 'ROUTE_CHANGE_EVENT'
---

<objective>
Add the side-by-side CSS grid layout to the host styles and verify the complete end-to-end infrastructure: dev server, dual mount, routing, event bridge, TypeScript, and HMR.

Purpose: Ensure all 7 INFRA requirements work together as a system -- not just individually. This is the phase gate.
Output: Verified working infrastructure with side-by-side layout ready for Phase 2 content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-scaffold/01-CONTEXT.md
@.planning/phases/01-infrastructure-scaffold/01-UI-SPEC.md
@.planning/phases/01-infrastructure-scaffold/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-scaffold/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add side-by-side grid layout + run automated verification suite</name>
  <files>
    packages/host/src/styles.css
  </files>
  <read_first>
    packages/host/src/styles.css (current state from Plan 01),
    packages/host/index.html (verify #app-shell, #vue-root, #react-root structure),
    .planning/phases/01-infrastructure-scaffold/01-UI-SPEC.md (Layout Contract: CSS Grid 2 equal columns, 1px border between panels, 100vh min-height, panel padding 16px)
  </read_first>
  <action>
    Update `packages/host/src/styles.css` to add the side-by-side grid layout after the existing TailwindCSS imports:

    Add these styles after the `@theme {}` block:

    ```css
    #app-shell {
      display: grid;
      grid-template-columns: 1fr 1fr;
      min-height: 100vh;
    }

    #vue-root {
      border-right: 1px solid #e2e8f0;
      min-height: 100vh;
    }

    #react-root {
      min-height: 100vh;
    }
    ```

    Per UI-SPEC: 2 equal columns, 1px solid slate-200 border between panels, 100vh min-height. Panel internal padding (p-4) is already handled by each app's root component.

    Then run the full automated verification suite:
    ```bash
    # 1. TypeScript check across all packages
    pnpm -r typecheck

    # 2. Start dev server in background, verify it starts
    pnpm dev &
    DEV_PID=$!
    sleep 5

    # 3. Verify HTML contains both root divs
    curl -s http://localhost:5173 | grep -q 'vue-root' && echo "vue-root: OK" || echo "vue-root: FAIL"
    curl -s http://localhost:5173 | grep -q 'react-root' && echo "react-root: OK" || echo "react-root: FAIL"

    # 4. Verify JS bundles are served (no 500 errors)
    curl -s -o /dev/null -w "%{http_code}" http://localhost:5173/src/main.ts

    # 5. Kill dev server
    kill $DEV_PID
    ```

  </action>
  <verify>
    <automated>cd /home/tungpt244/workspace/vibe && pnpm -r typecheck && echo "TYPECHECK OK" && timeout 15 bash -c 'pnpm dev &>/tmp/vite-dev.log & PID=$!; sleep 8; STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:5173); kill $PID 2>/dev/null; echo "DEV_SERVER_STATUS: $STATUS"; if [ "$STATUS" = "200" ]; then echo "DEV SERVER OK"; else cat /tmp/vite-dev.log | tail -20; fi'</automated>
  </verify>
  <acceptance_criteria>
    - packages/host/src/styles.css contains `#app-shell` with `display: grid` and `grid-template-columns: 1fr 1fr`
    - packages/host/src/styles.css contains `#vue-root` with `border-right: 1px solid #e2e8f0`
    - packages/host/src/styles.css still contains `@import 'tailwindcss'` and `@source` directives
    - `pnpm -r typecheck` exits with code 0
    - `pnpm dev` starts without errors (Vite dev server serves on port 5173)
    - `curl http://localhost:5173` returns HTML containing `vue-root` and `react-root`
  </acceptance_criteria>
  <done>Side-by-side grid layout applied. TypeScript passes across all 4 packages. Dev server starts and serves the page with both root mount points.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dual-mount, routing, event bridge, and HMR in browser</name>
  <files>none (verification only)</files>
  <action>
    Human verifies the complete Phase 1 infrastructure in the browser. No code changes -- this is a visual/functional verification checkpoint.

    Start the dev server if not running: `pnpm dev`

    **Test 1 -- Dual Mount (INFRA-01):**
    1. Open http://localhost:5173 in browser
    2. Verify TWO panels side-by-side: left has green "Vue 3" badge, right has cyan "React 19" badge
    3. Both panels show "Waiting for topic selection" text

    **Test 2 -- Routing + Event Bridge (INFRA-03, INFRA-04):**
    1. Navigate to http://localhost:5173/essentials/template-syntax
    2. Verify BOTH panels update with "essentials / template-syntax" route display + "Template vs JSX" content
    3. Navigate to http://localhost:5173/essentials/reactivity
    4. Verify BOTH panels update to show "Reactivity" content

    **Test 3 -- Direct URL Refresh (INFRA-04 race condition):**
    1. While on /essentials/template-syntax, press F5 (hard refresh)
    2. Verify BOTH panels still show correct topic (Vue URL fallback on mount)

    **Test 4 -- HMR (INFRA-02):**
    1. Edit packages/vue-app/src/topics/essentials/TemplateSyntax.vue -- change placeholder text
    2. Verify only Vue panel (left) updates without full page reload
    3. Edit packages/react-app/src/topics/essentials/TemplateSyntax.tsx -- change placeholder text
    4. Verify only React panel (right) updates without full page reload

  </action>
  <verify>
    <automated>cd /home/tungpt244/workspace/vibe && pnpm -r typecheck && echo "TYPECHECK PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Both #vue-root and #react-root visible side-by-side in browser at http://localhost:5173
    - Vue panel shows green "Vue 3" badge, React panel shows cyan "React 19" badge
    - Navigating to /essentials/template-syntax shows topic content on BOTH sides
    - Hard refresh on a topic URL preserves state on BOTH sides
    - Editing .vue file triggers HMR only on Vue side
    - Editing .tsx file triggers HMR only on React side
  </acceptance_criteria>
  <done>Human confirmed all 4 tests pass: dual mount visible, routing works on both sides, URL refresh maintains state, HMR updates independently per framework.</done>
</task>

</tasks>

<verification>
Phase 1 complete when ALL pass:
1. `pnpm -r typecheck` exits 0
2. `pnpm dev` starts without errors
3. Both #vue-root and #react-root render content side-by-side
4. URL /:category/:topicId renders correct topic on both sides
5. Direct URL refresh works (Vue URL fallback)
6. HMR works independently for both Vue and React
</verification>

<success_criteria>
All 7 INFRA requirements verified:

- INFRA-01: Both frameworks mount side-by-side on same page
- INFRA-02: Vite handles both .vue and .tsx in same build
- INFRA-03: React Router handles /:category/:topicId
- INFRA-04: Vue syncs via CustomEvent (including URL fallback)
- INFRA-05: Shared registry used by both apps
- INFRA-06: pnpm monorepo with 4 packages working
- INFRA-07: TypeScript passes across all packages
  </success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-scaffold/01-03-SUMMARY.md`
</output>
