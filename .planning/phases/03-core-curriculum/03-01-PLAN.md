---
phase: 03-core-curriculum
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/topics.ts
  - packages/react-app/src/topics/essentials/Reactivity.tsx
  - packages/react-app/src/topics/essentials/Computed.tsx
  - packages/react-app/src/topics/essentials/Watchers.tsx
  - packages/react-app/src/topics/essentials/Lifecycle.tsx
  - packages/react-app/src/topics/essentials/ClassStyleBindings.tsx
  - packages/react-app/src/topics/essentials/TemplateSyntax.tsx
  - packages/vue-app/src/topics/essentials/Reactivity.vue
  - packages/vue-app/src/topics/essentials/Computed.vue
  - packages/vue-app/src/topics/essentials/Watchers.vue
  - packages/vue-app/src/topics/essentials/Lifecycle.vue
  - packages/vue-app/src/topics/essentials/ClassStyleBindings.vue
  - packages/vue-app/src/topics/essentials/TemplateSyntax.vue
autonomous: true
requirements:
  - ESS-04
  - ESS-05
  - ESS-06
  - ESS-07
  - ESS-09

must_haves:
  truths:
    - "Topic registry contains all 22 new topic entries with correct category, slug, and order"
    - "All 12 Phase 2 topic files use p-4 for code block padding (not p-3)"
    - "All 12 Phase 2 sidebar topic items use py-2 (not py-1.5) via Sidebar.tsx update"
  artifacts:
    - path: "packages/shared/src/topics.ts"
      provides: "28 total topic entries (6 existing + 22 new) across 4 categories"
      contains: "conditional-rendering"
  key_links:
    - from: "packages/shared/src/topics.ts"
      to: "packages/react-app/src/components/Sidebar.tsx"
      via: "getAllCategories() and getTopicsByCategory()"
      pattern: "getAllCategories|getTopicsByCategory"
---

<objective>
Expand the shared topic registry with all 22 new topic entries for Phase 3 and backfill spacing inconsistencies in the 12 existing Phase 2 topic files.

Purpose: The registry is the single source of truth for sidebar rendering and topic routing. All subsequent content plans depend on these entries existing. Spacing backfill ensures visual consistency between old and new topics.
Output: Updated topics.ts with 28 entries, 12 Phase 2 files with corrected spacing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-curriculum/03-RESEARCH.md
@.planning/phases/03-core-curriculum/03-UI-SPEC.md

@packages/shared/src/topics.ts
@packages/shared/src/types.ts
@packages/shared/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand topic registry with 22 new entries</name>
  <files>packages/shared/src/topics.ts</files>
  <read_first>
    - packages/shared/src/topics.ts (current 6 entries — see structure for id/slug/title/category/order/description pattern)
    - packages/shared/src/types.ts (Category type union — valid values: 'essentials' | 'components' | 'reusability' | 'built-in' | 'scaling-up' | 'deep-dive')
    - packages/shared/src/constants.ts (CATEGORIES record — keys are the category slugs)
  </read_first>
  <action>
Add 22 new TopicMeta entries to the `topics` array in `packages/shared/src/topics.ts`. Keep existing 6 entries unchanged. Add entries in this exact order after the existing 6:

**Essentials (category: 'essentials', order 7-11):**
- `{ id: 'conditional-rendering', slug: 'conditional-rendering', title: 'Conditional Rendering', category: 'essentials', order: 7, description: 'v-if/v-else/v-show vs &&/ternary/early return' }`
- `{ id: 'list-rendering', slug: 'list-rendering', title: 'List Rendering', category: 'essentials', order: 8, description: 'v-for + :key vs .map() + key' }`
- `{ id: 'event-handling', slug: 'event-handling', title: 'Event Handling', category: 'essentials', order: 9, description: '@click/@input + modifiers vs onClick/onChange' }`
- `{ id: 'form-bindings', slug: 'form-bindings', title: 'Form Bindings', category: 'essentials', order: 10, description: 'v-model vs controlled components' }`
- `{ id: 'template-refs', slug: 'template-refs', title: 'Template Refs', category: 'essentials', order: 11, description: 'ref + template ref vs useRef' }`

**Components In-Depth (category: 'components', order 1-7):**
- `{ id: 'props', slug: 'props', title: 'Props', category: 'components', order: 1, description: 'defineProps<T>() vs props type annotation' }`
- `{ id: 'events-callbacks', slug: 'events-callbacks', title: 'Events / Callbacks', category: 'components', order: 2, description: 'emit() vs callback props' }`
- `{ id: 'component-v-model', slug: 'component-v-model', title: 'Component v-model', category: 'components', order: 3, description: 'defineModel() vs controlled + callback' }`
- `{ id: 'fallthrough-attributes', slug: 'fallthrough-attributes', title: 'Fallthrough Attributes', category: 'components', order: 4, description: 'Auto forward vs spread ...rest props' }`
- `{ id: 'slots', slug: 'slots', title: 'Slots', category: 'components', order: 5, description: 'Default/named slots vs children + render props' }`
- `{ id: 'provide-inject', slug: 'provide-inject', title: 'Provide / Inject', category: 'components', order: 6, description: 'provide()/inject() vs createContext/useContext' }`
- `{ id: 'async-components', slug: 'async-components', title: 'Async Components', category: 'components', order: 7, description: 'defineAsyncComponent vs React.lazy + Suspense' }`

**Reusability (category: 'reusability', order 1-3):**
- `{ id: 'composables-hooks', slug: 'composables-hooks', title: 'Composables / Hooks', category: 'reusability', order: 1, description: 'Composables vs custom hooks' }`
- `{ id: 'custom-directives', slug: 'custom-directives', title: 'Custom Directives', category: 'reusability', order: 2, description: 'directive() vs hooks/HOC (Vue-only concept)' }`
- `{ id: 'plugins', slug: 'plugins', title: 'Plugins', category: 'reusability', order: 3, description: 'app.use() vs Context/Provider pattern' }`

**Built-in Components (category: 'built-in', order 1-5):**
- `{ id: 'transition', slug: 'transition', title: 'Transition', category: 'built-in', order: 1, description: '<Transition> vs CSS transitions' }`
- `{ id: 'transition-group', slug: 'transition-group', title: 'TransitionGroup', category: 'built-in', order: 2, description: '<TransitionGroup> vs manual list animation' }`
- `{ id: 'keep-alive', slug: 'keep-alive', title: 'KeepAlive', category: 'built-in', order: 3, description: '<KeepAlive> vs manual state preservation' }`
- `{ id: 'teleport', slug: 'teleport', title: 'Teleport', category: 'built-in', order: 4, description: '<Teleport> vs createPortal' }`
- `{ id: 'suspense', slug: 'suspense', title: 'Suspense', category: 'built-in', order: 5, description: '<Suspense> vs <Suspense>' }`

Note on BTIN-01 description: Use "CSS transitions" instead of "framer-motion" since framer-motion is NOT installed in the project.

After adding, run `pnpm prettier --write packages/shared/src/topics.ts`.
  </action>
  <verify>
    <automated>cd /home/tungpt244/workspace/vibe && node -e "const t = require('./packages/shared/src/topics.ts'); console.log('FAIL: cannot require ts')" 2>/dev/null; grep -c "id:" packages/shared/src/topics.ts | grep -q "28" && echo "PASS: 28 entries" || echo "FAIL: not 28 entries"</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/topics.ts contains exactly 28 objects in the topics array (6 existing + 22 new)
    - grep "category: 'components'" packages/shared/src/topics.ts returns 7 lines
    - grep "category: 'reusability'" packages/shared/src/topics.ts returns 3 lines
    - grep "category: 'built-in'" packages/shared/src/topics.ts returns 5 lines
    - grep "category: 'essentials'" packages/shared/src/topics.ts returns 11 lines (6 old + 5 new)
    - Every new entry has id, slug, title, category, order, description fields
    - pnpm -r typecheck passes with 0 errors
  </acceptance_criteria>
  <done>Topic registry has 28 entries spanning 4 categories. getAllCategories() returns ['essentials', 'components', 'reusability', 'built-in']. All new slugs follow the toPascalCase convention for file discovery.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill Phase 2 spacing — p-3 to p-4, py-1.5 to py-2, active font-medium to font-semibold</name>
  <files>
    packages/react-app/src/topics/essentials/Reactivity.tsx
    packages/react-app/src/topics/essentials/Computed.tsx
    packages/react-app/src/topics/essentials/Watchers.tsx
    packages/react-app/src/topics/essentials/Lifecycle.tsx
    packages/react-app/src/topics/essentials/ClassStyleBindings.tsx
    packages/react-app/src/topics/essentials/TemplateSyntax.tsx
    packages/vue-app/src/topics/essentials/Reactivity.vue
    packages/vue-app/src/topics/essentials/Computed.vue
    packages/vue-app/src/topics/essentials/Watchers.vue
    packages/vue-app/src/topics/essentials/Lifecycle.vue
    packages/vue-app/src/topics/essentials/ClassStyleBindings.vue
    packages/vue-app/src/topics/essentials/TemplateSyntax.vue
    packages/react-app/src/components/Sidebar.tsx
  </files>
  <read_first>
    - packages/react-app/src/topics/essentials/Reactivity.tsx (reference — has p-3 on line 74 that must become p-4)
    - packages/react-app/src/components/Sidebar.tsx (has py-1.5 on line 67 and font-medium on line 69)
    - packages/react-app/src/topics/essentials/ClassStyleBindings.tsx (has h2 outside demo box — note deviation but do NOT restructure, only fix spacing)
    - packages/vue-app/src/topics/essentials/Reactivity.vue (Vue equivalent — check for p-3)
  </read_first>
  <action>
In ALL 12 topic files (6 React .tsx + 6 Vue .vue in packages/*/src/topics/essentials/):

1. Replace `p-3` with `p-4` in the code block `<pre>` className/class. The target line pattern is:
   - React: `className="mt-2 bg-slate-900 text-slate-100 text-xs p-3 rounded overflow-x-auto"` → change `p-3` to `p-4`
   - Vue: `class="mt-2 bg-slate-900 text-slate-100 text-xs p-3 rounded overflow-x-auto"` → change `p-3` to `p-4`

In `packages/react-app/src/components/Sidebar.tsx`:

2. Change `py-1.5` to `py-2` in the topic button className (line 67 area): `'w-full text-left px-3 py-1.5 text-sm` → `'w-full text-left px-3 py-2 text-sm`

3. Change `font-medium` to `font-semibold` in the active topic styling (line 69 area): `'bg-blue-50 text-blue-700 font-medium'` → `'bg-blue-50 text-blue-700 font-semibold'`

4. Add `aria-label` to the collapse toggle button: add `aria-label={collapsed ? 'Expand sidebar' : 'Collapse sidebar'}` alongside the existing `title` attribute.

Run `pnpm prettier --write` on each modified file after editing.
  </action>
  <verify>
    <automated>cd /home/tungpt244/workspace/vibe && grep -r "p-3" packages/react-app/src/topics/ packages/vue-app/src/topics/ | grep -v node_modules | grep -c "p-3" | grep -q "^0$" && echo "PASS: no p-3 remaining" || echo "FAIL: p-3 still found"; grep "py-1.5" packages/react-app/src/components/Sidebar.tsx | grep -c "py-1.5" | grep -q "^0$" && echo "PASS: no py-1.5" || echo "FAIL: py-1.5 still found"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "p-3" packages/react-app/src/topics/ returns 0 matches
    - grep -r "p-3" packages/vue-app/src/topics/ returns 0 matches
    - grep "py-1.5" packages/react-app/src/components/Sidebar.tsx returns 0 matches
    - grep "font-medium" packages/react-app/src/components/Sidebar.tsx returns 0 matches
    - grep "aria-label" packages/react-app/src/components/Sidebar.tsx returns at least 1 match
    - grep "py-2" packages/react-app/src/components/Sidebar.tsx returns at least 1 match
    - grep "font-semibold" packages/react-app/src/components/Sidebar.tsx returns at least 1 match for active state
    - pnpm -r typecheck passes with 0 errors
  </acceptance_criteria>
  <done>All Phase 2 files use p-4 for code blocks, sidebar uses py-2 for topic items and font-semibold for active state, collapse button has aria-label.</done>
</task>

</tasks>

<verification>
- `pnpm -r typecheck` passes with 0 errors
- `grep -c "id:" packages/shared/src/topics.ts` returns 28
- `grep -r "p-3" packages/*/src/topics/` returns 0 matches
- `grep "py-1.5" packages/react-app/src/components/Sidebar.tsx` returns 0 matches
- `pnpm dev` starts without errors and sidebar shows 4 categories
</verification>

<success_criteria>
Topic registry has 28 entries. Sidebar displays all 4 categories (Essentials, Components In-Depth, Reusability, Built-in Components). All Phase 2 topic files use p-4 spacing. Sidebar topic items use py-2 and font-semibold for active state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-curriculum/03-01-SUMMARY.md`
</output>
